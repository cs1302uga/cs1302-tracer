name: CI / Release

on:
  push:
    branches: [ "main" ]
    tags:
      - '*'
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  documentation:
    name: Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: website/package-lock.json
      - name: Build tracer
        run: ./mvnw -B -ntp package -DskipTests
      - name: Check executable documentation
        run: |
          python3 scripts/check_docs.py
          python3 scripts/docs_release.py check
          python3 -m unittest discover -s scripts -p 'test_*.py'
      - name: Retain existing documentation versions
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: python3 scripts/docs_release.py check --base "$BASE_SHA"
      - name: Verify release snapshot and ancestry
        if: startsWith(github.ref, 'refs/tags/')
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          git merge-base --is-ancestor HEAD origin/main
          python3 scripts/docs_release.py check --tag "$RELEASE_TAG"
      - run: npm --prefix website ci
      - run: npm --prefix website run build
      - run: python3 scripts/check_site.py
      - name: Save reviewable site
        uses: actions/upload-artifact@v4
        with:
          name: documentation-preview
          path: website/build
          retention-days: 7

  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest ]
        java: [ '21', '25' ]
    steps:
      - uses: actions/checkout@v7
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v6
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: maven
      - name: Build and test with Maven
        run: mvn clean package
      - name: Verify trace compatibility
        run: |
          python3 -m unittest discover -s examples -p test_verify.py
          python3 examples/verify.py

  release:
    permissions:
      contents: write
    needs: [test, documentation]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
      - name: Set up JDK 21
        uses: actions/setup-java@v6
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
      - name: Build with Maven
        run: mvn clean package -DskipTests
      - run: |
          mv target/code-tracer*.jar target/code-tracer.jar
      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          files:
            target/*.jar
